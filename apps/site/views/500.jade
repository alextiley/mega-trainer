extend /layouts/default

block title
	title Internal server error (500) | node-mvc-boilerplate

block content
	h2 Internal server error (500)
	p Sorry, but somebody has broken the site. Please forward the message below to somebody important:
	if error.msg
		pre #{error.msg}
		hr
	if error.stack
		pre #{error.stack}